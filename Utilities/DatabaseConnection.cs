public static class DatabaseConnection
{
    public static string GetConnectionString()
    {
        return "Host=80.217.81.80:44448;" + "Database=finance_app;" + "Username=postgres;" + "Password=783259;" + "Include Error Detail=true;";
    }
}
